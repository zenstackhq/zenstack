datasource db {
    provider = 'sqlite'
    url      = 'file:./dev.db'
}

enum PostStatus {
    DRAFT
    ACTIVE
    CANCELLED
}

model User {
    id Int @id @default(autoincrement())
}

model Post {
    id     String @id
    status String

    @@validate(status in PostStatus)
}